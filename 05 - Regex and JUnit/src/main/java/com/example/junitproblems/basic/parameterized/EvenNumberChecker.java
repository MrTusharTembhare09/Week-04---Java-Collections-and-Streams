package com.example.junitproblems.basic.parameterized;

// Creating EvenNumberChecker class to test parameterized values
public class EvenNumberChecker {
    // Checking if a number is even
    public static boolean isEven(int number) {
        return number % 2 == 0;
    }
}